#include "structs.h"
#include <stdio.h>



int main (int argc, char *argv[]) {
    Record record;
    Catalog catalog;

    // Cargar el registro y el catálogo (structs record y catalog)

    // Procesar requisitos y correquisitos para verificar que cursos puede tomar el estudiante

    // Procesar los horarios de los cursos y grupos para verificar choques de horario

    // Generar el archivo final en base al catalog generado 

    return 0;
}